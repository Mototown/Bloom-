# Bloom

**Decision-aware dbt model generator.**

Most AI-generated dbt models are disposable SQL. Bloom makes the *decision* first-class: every model ships with a structured Architecture Decision Record (ADR) that explains *why* the model exists, what alternatives were considered, and what the consequences are.

The decision travels with the code.